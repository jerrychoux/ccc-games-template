import { _decorator, Component, Node } from "cc";
const { ccclass, property } = _decorator;

@ccclass("InfiniteListItem")
export class InfiniteListItem extends Component {
  start() {}

  update(deltaTime: number) {}

  updateData(data: number) {}
}
